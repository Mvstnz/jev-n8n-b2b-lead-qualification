# Preflight (redacted)

Checked 24 September 2026. The n8n Streamable HTTP MCP endpoint responded to `initialize` and `tools/list` with protocol `2025-03-26`; the instance UI reported n8n 2.41.2. Six demo workflows were created and remain inactive: mock showcase, protected Intake Preview, protected Intake pipeline, mock Test Runner, native JEV Evaluation Core, and private Slack sender. No public trigger is active. The sender's approval gate ran without reaching its HTTP send node.

Before live traffic, the authenticated Vercel credits API reported **USD 5 balance and USD 0 used**. The dedicated `n8n key` received a **USD 1 API-key budget with no refresh**, verified in the Vercel CLI. This lifetime safety ceiling sits within the available free credits; the project still defaults to **USD 0 paid-API budget**. Vercel documents that budgets are soft at the crossing request. The private runtime also caps each JEV request estimate at USD 0.01 and the local campaign stops if the credit balance falls below USD 4.50. All valid sampled JEV responses reported `gateway.cost = 0` under the current promotion. No credits were purchased and auto top-up was not enabled by this project. The account's auto top-up setting was not independently read.

The first native smoke attempt returned HTTP 403 `customer_verification_required`. After account setup, a second smoke request succeeded with a validated `typesafe-ai/jev` response. One valid synthetic form execution completed n8n Evaluation Core, appended a Google Sheets row, built a Slack preview and returned a receipt. The row was independently read back through the Codex Google connector. No Slack webhook request was made. Earlier credential listing/UI had shown the Sheets action credential as needing setup; the later successful runtime append confirms it now works.

The private n8n Evaluation Core currently passes the Vercel key in its HTTP node headers because the exposed n8n MCP cannot create credentials. That workflow is inactive. The public SDK source is credential-free and fail-closed. Before production activation, move the header into an n8n HTTP credential and implement the duplicate and failure controls in [limitations](limitations.md). Private URLs, IDs, raw replies and customer fields are excluded from this report.
