# Limitations and next gates

1. All six demo workflows remain inactive. The protected Intake pipeline completed one synthetic form-to-JEV-to-Sheets-to-preview execution, but it is still a draft. Its public template uses placeholders for private workflow and Sheet IDs.
2. The n8n Google Sheets action credential works: a synthetic lead was appended and independently read back. `TestRuns` was not populated by n8n; Sheets failure/recovery behavior was not proven.
3. The n8n Evaluation Core currently contains the Vercel authorization header in a **private inactive workflow** because the available n8n MCP can list credentials but cannot create one. The key is never in the public repository. Replace the inline header with a native n8n HTTP credential before operational activation. Public SDK source remains fail-closed with USD 0 paid budget.
4. Provider rate limiting left only 28/90 valid live JEV replies. The other 62 slots ended in 429/503 or a `Retry-After` beyond the adapter's two-second bound. Full-set route quality and three-run stability remain unmeasured. The key has a USD 1 non-resetting budget within USD 5 free credits; no payment was made and sampled actual costs were USD 0. The account auto top-up setting was not independently read.
5. Five MOCK Slack previews and one proposed summary exist. No webhook message was sent. The private sender's approval gate remains closed pending combined six-message approval.
6. Idempotency, concurrent duplicate prevention, lost-response recovery, Sheets failure handling and unknown Slack delivery have not been proven end to end. The draft uses a simple append and is not safe for parallel duplicate submissions. Keep it inactive until these controls and credential migration are complete.
7. The dataset and policy are fictional portfolio material. Labels are not sales-team validated, scores are not win probabilities, and no real ROI or production readiness is implied.
