# Instructions for this public project

Build and test the B2B workflow using the versioned policy, questions and fixtures.
Keep all output in English. This is a synthetic portfolio demo, not a live sales system.
Never infer model performance from mock/reference tests. Do not tune on evaluation labels.

Do not add credentials, private account URLs, unredacted workflows or local operational
logs here. Plugin access for the coding agent is distinct from runtime access in n8n.
Use environment/credential examples only; Google/Slack/AI Gateway secrets stay private.

Before publication run policy tests, release-guard tests, and the release guard.
The private-workspace instructions, if supplied by the owner outside this repository,
specify access and publication permissions. Do not assume authority over other resources.
