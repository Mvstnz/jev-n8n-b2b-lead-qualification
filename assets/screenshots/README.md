# Screenshots: real evidence only

The four PNGs here are real UI captures from 24 September 2026. `workflow.png`
shows the inactive n8n intake canvas with its private subworkflow ID obscured.
`sheets-leads.png` shows five synthetic live JEV intake rows. `sheets-summary.png`
shows their summary alongside the separate 30-case evaluation. `slack-mock-demo.png`
shows two of the six delivered, clearly labelled MOCK messages.

The final images were cropped, visually reviewed, converted to metadata-free RGB
PNGs and hashed in `checks/media_review.json`. The release scanner verifies those
hashes, not pixels. Do not add fabricated evidence, account details, browser
runtime URLs, secrets or the private setup ZIP. See `tools/README.md`.
